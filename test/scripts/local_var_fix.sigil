
# Test cases for fixing how local variables work.
# The assumption that all values left on the stack are locals (after each line) is not true for the grammar

var f = fn(){ print("a"+{ var b = "b"; var c = "c"; b+c }); };
f();

var g = fn(){ print(for i in 0:3 {""+i} + for i in 6:9 {""+i}); };
g();

echo ""+for i in 1:5 {2*i};

var h = fn(){ echo ""+for i in 1:5 {2*i}; };
h();
