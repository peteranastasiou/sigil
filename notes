
Documentation TODO:
addition matrix
number + number -> number
string + string -> string
string + number -> string (serialise)
string + list -> string (serialise)
list + list -> list (concatenation)
list + anything -> list (append)
any other combination is invalid
How to do a string or list pre-pend?

expecting an expression block: -- canBeExpression=true
    ( <statement>; )*          -- canBeExpression=true, but check it isn't
    <expression>               -- canBeExpression=true, check it is!

expecting a statement block:
    ( <statement>; )*          -- canBeExpression=false


# Ensure for expression works within an expression block!

const a = for i in 0:10 {
    
} + 5;


This is broken!
const abcd = [1,2,3] + [4,5];
> print(abcd);
[???, 2, 3, ???, 5]



